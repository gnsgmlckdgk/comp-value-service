server:
  port: 18080
  servlet:
    context-path: /dart
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: FinReport

logging:
  level:
    root: INFO               # 기본 로그레벨
#    com.finance.dart: DEBUG  # 특정 패키지


